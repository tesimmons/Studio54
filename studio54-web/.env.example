# Studio54 Web UI Environment Variables

# API URL (leave empty to use same host as web UI)
# For development, use: http://localhost:8010/api/v1
# For production with Docker: /api/v1 (proxied by nginx)
VITE_API_URL=/api/v1
